{
  "name": "gh-action-event-poc",
  "version": "1.0.0",
  "description": "Just testing out some gh action workflow options using events.",
  "main": "index.js",
  "scripts": {
    "watch": "rollup ./index.js --file ./build/bundle.js --format es --watch",
    "build": "rollup ./index.js --file ./build/bundle.js --format es",
    "start": "node ./index.js",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "emoji-poop": "^1.3.0"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "rollup": "^2.48.0"
  }
}
